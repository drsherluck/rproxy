Small reverse proxy server using docker and kubernetes.
Using minikube on local machine.

run certificates.sh to generate certificates
run run.sh to create images and start k8s

use curl -sl --cacert ca.crt https://localhost:5000
